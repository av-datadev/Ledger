/// <reference types="vite/client" />

// Build stamp injected by vite.config.ts (`define`), shown on the Data tab so
// the running build is identifiable on a device.
declare const __BUILD_ID__: string;
